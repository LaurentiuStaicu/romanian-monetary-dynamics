name: BNR Household DSTI Workbook Access Probe

# Historical source acquisition is retained immutably in-repository.
# Manual reruns of the same frozen workbook URLs are reproduction/source-access
# checks only. An unchanged rerun is not a new vintage and does not reopen the
# household-consumption mechanism; genuinely new provider evidence must satisfy
# the declared population-alignment/target reopen conditions before new work.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  probe-bnr-dsti-workbooks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - name: Verify frozen probe contract and signature logic
        run: python -m unittest tests.test_bnr_household_dsti_workbook_probe -v
      - name: Probe exact official workbook URLs
        env:
          BNR_DSTI_WORKBOOK_PROBE_OUT: bnr_dsti_workbook_probe_artifacts
        run: python scripts/audit_bnr_household_dsti_workbook_probe.py
      - name: Upload source-access evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: bnr-household-dsti-workbook-probe-${{ github.sha }}
          path: bnr_dsti_workbook_probe_artifacts/
          if-no-files-found: error
          retention-days: 90
