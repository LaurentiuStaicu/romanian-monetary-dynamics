# Changelog

All notable public scientific-core releases of Romanian Monetary Dynamics (RMD) are recorded here.

## Unreleased

### Repository governance

- clarified the provenance terminal-closure handoff in `STATUS.md` so the 2026-09-22 AMECO-access horizon is explicitly historical and current continuation follows the canonical model-contract monitoring pointer; the v0.10 audit now rejects regression to the stale present-tense wording, with no trigger, scientific state, data value, behavioural state or release-version change;
- synchronized the model-contract monitoring-status summary with canonical monitoring horizon v0.10, including the November 2026 AMECO window and the two October QSA publication milestones, and added a regression audit so the textual summary cannot drift behind the canonical horizon pointer; no trigger, scientific state, data value or release version changed;
- synchronized the canonical release-versioning rationale after the execution-governance terminal closure, GitHub Actions full-SHA/Dependabot supply-chain hardening, AMECO November timing refinement and ECB QSA October release-context clarification; these remain Unreleased governance/monitoring progress inside the existing four change classes, while public/repository v0.3.0, `version_bump_required_now = false` and v0.4.0 as the default next substantive milestone remain unchanged;
- clarified the October 2026 ECB QSA monitoring context: the official calendar has separate Q2 releases for households/non-financial corporations on 2 October and the broader economic/financial developments by institutional sector on 28 October; the monitoring successor records both dates and explicitly preserves the rule that routine QSA publication milestones are not Accounting Spine reopen triggers without changed topology or a genuinely new source; no scientific/data/version state changes;
- narrowed the fiscal AMECO trigger-monitoring window using new official Commission timing evidence: the Autumn 2026 Economic Forecast is officially expected in November 2026, so the successor monitoring horizon forbids pre-November release-presence polling while deliberately leaving the exact publication day unresolved; all trigger identities, AMECO dataset-identity rules, modelling gates and public v0.3.0 state remain unchanged;
- hardened the GitHub Actions dependency supply chain: all external workflow actions are now restricted to the three already-used GitHub-owned actions and pinned to full release commit SHAs with same-line semantic-version comments; `actions/upload-artifact` is upgraded from v4 to current v7.0.1, and weekly Dependabot `github-actions` updates keep pinned SHAs current; all workflows remain on GitHub-hosted `ubuntu-latest`, and no scientific, data, trigger, permission or version state changes;
- formally closed the post-v0.3.0 repository execution-governance hardening series at its current scope with a machine-readable terminal assessment anchored through PR #224 / commit `c5b890483db8398807264b39341c80bfd61dfdc2`; the model contract and scientific baseline now register the same authority, and regression coverage freezes the 73-workflow / four-write-workflow / zero-`pull_request_target` closure snapshot while declaring explicit reopen conditions instead of encouraging repeated governance churn; no scientific, data or version state changes;
- forbids `pull_request_target` repository-wide in the central GitHub Actions governance audit after an exhaustive 73-workflow scan confirmed that RMD does not use this privileged event; ordinary `pull_request` remains the review trigger, while any future need for elevated PR context must first require an explicit governance redesign rather than silently entering the workflow surface; no scientific, data or version state changes;
- synchronized the release-versioning rationale after the post-v0.3.0 execution-governance hardening series: the canonical Unreleased state now explicitly records least-privilege Actions permissions, manual/evidence-triggered live-provider boundaries, fully offline Scientific CI, audit-script execution coverage and executable-command-aware PR provider-access regression protection as a fourth change class, while preserving public/repository v0.3.0, `version_bump_required_now = false` and v0.4.0 as the default next substantive milestone; no scientific or version surface changed;
- repaired the generic pull-request live-provider regression gate so it now matches real `scripts/*.py` command references and inspects only executable `run:` content rather than path filters or comments; this closes the script-reference parsing defect in the first generic gate while preserving legitimate offline PR workflows such as F3 materialisation, whose historical live audit script appears only in trigger paths and is not executed; scientific/public state remains unchanged;

- removed seven historical branch-bound live-source jobs from `scientific-ci.yml` after their PR cycles closed; the generic pull-request provider-access regression gate now covers Scientific CI without a whole-file exemption, the retained Eurostat/OECD counterpart and BNR BLS recovery paths remain available through dedicated manual trigger-conditioned workflows, and four completed source-dependent reference-mode gates retain execution coverage only through a read-only, explicitly acknowledged historical `workflow_dispatch` replay path; the public/scientific state remains unchanged;

- generalized the live-source trigger hardening into a repository-wide regression invariant: apart from `scientific-ci.yml`'s separately conditioned/manual subjobs, any workflow with a `pull_request` trigger now fails tests if it directly invokes a Python script containing provider-network primitives or an inline `curl`/`wget` command; this prevents future code-review events from silently reintroducing live scientific-source polling while preserving offline reproduction workflows and explicitly manual/evidence-triggered source paths;
- closed a stale corporate-investment execution boundary after the preregistered 2022Q1-2023Q4 structural-selection cycle had already produced and frozen `FAIL_BEFORE_HOLDOUT`: the one-shot gate is now recorded as consumed (`selection_execution_authorized=false`, `selection_execution_consumed=true`), the former execution workflow is an offline integrity check rather than a re-estimation path, and the three retained financing/source/supplemental provider workflows plus their direct CLIs are manual/explicit-refetch only; the 2024Q1-2026Q1 final evaluation remains unopened and no behavioural form, parameter, feedback or public version is activated;
- completed a terminal Accounting-Spine live-refresh trigger sweep: fourteen retained ECB/Eurostat F2-F8/F3 source/coverage workflows that still performed provider access on pull-request edits are now manual-only, matching the canonical `EVIDENCE_TRIGGERED_ACCOUNTING_SPINE_HOLD` and `no_repeated_live_probe_without_trigger` rule; the central live-source regression test now covers these workflows so code-review events cannot silently become new provider probes, while all retained assessments, source vintages, materialized cells, blockers and public v0.3.0 state remain unchanged;
- aligned the original F2 Phase-A0 ECB QSA source-structure probe with the terminal Accounting Spine hold: its workflow is now manual-only rather than triggered by pull-request edits, and the direct CLI refuses provider access unless `--allow-live-refetch` is supplied explicitly; regression coverage enforces both boundaries, while the retained 500-request source-structure assessment, F2M materialisation, F21 blocker, closed reopen gate and public v0.3.0 state remain unchanged;
- hardened the five retained live-acquisition utilities whose completed write-capable workflows were already retired: BNR government-issuance acquisition, ECB supply-load denominator probing, MoF Q1 announced-supply acquisition, MoF realized-financing source probing and MoF supply-pressure probing now refuse network access by default and require an explicit `--allow-live-refetch` acknowledgement; regression tests execute each CLI without that flag and require failure before provider access, while pure parsing/validation functions remain importable for offline tests and historical provenance; no retained bytes, trigger, scientific gate or public version changed;
- retired the unreferenced executable `scripts/audit_fx_hicp_source_structure.py` probe for archived/discontinued `prc_hicp_midx` / ECOICOP-v1 after confirming that the frozen FX-inflation source boundary and current manual workflow use only `prc_hicp_minr` / ECOICOP-v2; added a repository-wide Scientific CI execution-coverage gate requiring every `scripts/audit_*.py` to be referenced by a workflow and every workflow audit reference to resolve to an existing script, plus a source-specific regression guard preventing the legacy HICP route from returning; retained source bytes, mechanism state, calibration hold and public v0.3.0 are unchanged;
- extended the GitHub Actions permission boundary so every workflow must declare a top-level `permissions:` policy explicitly; the current 72-workflow surface already satisfies this rule, so future workflows can no longer inherit repository/org token defaults silently while the exact four-workflow write allowlist remains unchanged;
- added a repository-wide GitHub Actions write-permission boundary audit: all workflow `*: write` grants are now machine-checked against an exact four-workflow allowlist (three manual evidence-triggered MoF recovery retention/materialisation workflows plus the authorization-gated release publisher job); any new write-capable workflow, `permissions: write-all`, non-`contents` write scope, automatic trigger on the MoF write paths, or global publisher write grant now fails Scientific CI;
- hardened `publish-authorized-release.yml` to least privilege: the workflow now defaults to `contents: read`, a separate read-only authorization job inspects the exact green `main` commit and exposes release metadata, and only a downstream job gated by `publication_authorized == true` receives job-scoped `contents: write`; the current v0.3.0 authorization remains consumed/inactive, so normal green CI runs cannot receive release-write capability, and Scientific CI now regression-checks this topology;
- reduced `f4-partial-materialization-audit.yml` to read-only reproduction: the exact Phase-B artifact/vintage was already retained on 2026-09-18 and the later structural-zero successor explicitly regenerates offline from that immutable source, so the legacy PR bootstrap step that could re-download the historical Actions artifact and `git push` it was removed; the workflow still verifies the retained vintage, regenerates the historical 15/15 partial artifact offline and requires semantic equivalence, with regression coverage forbidding restoration of the write path;
- hardened the still-open full-2025 Ministry announced-RON reference-auction recovery path without closing it: trigger-aware monitoring horizon 0.8 / scientific baseline manifest 1.12 preserve the three unresolved official acts and their frozen reopen conditions, disable automatic `push` polling across the four recovery workflows, require manual dispatch with declared trigger/evidence/verification, and pin checkout to the correct source-vintage or recovery branch; write permissions remain only on the three workflows that must commit exact evidence after a genuine trigger, while the probe remains read-only; no reference-mode promotion, estimation, feedback activation or public-version change was authorized;
- retired six completed branch-push workflows that could still perform live acquisition/materialisation with `contents: write` on historical source-vintage branches (BNR government issuance, ECB supply-load denominator candidates, MoF Q1 announced-supply probe/materialisation, MoF realized-financing channels and MoF supply-pressure probe); replaced them with one manual read-only offline verifier over the already-retained canonical assessments/inventory and added regression coverage proving each underlying retention stage is complete; the still-open full-2025 MoF three-source recovery path is intentionally excluded from this retirement and remains evidence-triggered;
- retired the completed write-capable `fiscal-capb-raw-source-preservation.yml` creator after the no-refetch CAPB preservation assessment had already frozen branch `source-vintage/fiscal-capb-raw-2026-09-20` at commit `efb1f3ae…`; replaced it with a manual read-only verifier that pins the branch/commit and manifest blob and can recompute byte counts/SHA-256 for all 31 retained AMECO release archives without provider refetch, with regression coverage preventing restoration of the write path; no retained raw evidence, fiscal mechanism state or public version changed;
- retired the two completed write-capable BNR BLS missing-round promotion workflows (`promote-bnr-bls-missing-round-vintage.yml` and `promote-bnr-bls-missing-round-cell-extraction-vintage.yml`) after confirming that both destination vintages are already retained; replaced them with one manual read-only verifier for workbook bytes, extraction JSON, frozen review manifests and source-vintage inventory, eliminating a stale `pull_request:synchronize` workflow that still woke on unrelated PR updates; no retained bytes, canonical panel, scientific gate or public-version state changed;
- retired the completed, misleadingly named write-capable `.github/workflows/promote-corporate-investment-vintage.yml` path after confirming that it actually promoted the already-retained BNR BLS May-2025 XLSX vintage; replaced it with a correctly named manual read-only verifier that checks the frozen promotion review, retained file hashes/signatures and source-vintage inventory, and added a regression test forbidding restoration of the old PR-label/write path; no source bytes, scientific state or public version changed;
- completed the remaining manual-refresh wording alignment after baseline 1.11: the BNR household-DSTI probe and FX-inflation monthly materialiser now state explicitly that unchanged reruns are reproduction rather than automatic new-vintage evidence, and that source refresh alone does not satisfy their declared behavioural reopen conditions; the baseline audit now enforces these safeguards, with no scientific-state or public-version change;
- advanced the canonical trigger-aware monitoring horizon to a v0.7 `post_counterpart_workflow_boundary` successor without changing any trigger identity, date or evidence window: the retained OECD/Eurostat counterpart-discovery workflows are now explicitly trigger-conditioned historical replay paths under `accounting_counterpart_topology`, unchanged-topology reruns are reproducibility rather than scientific progress, the frozen 19 September preregistration contracts remain unmodified, and scientific baseline manifest 1.11 now distinguishes trigger-conditioned new-vintage evidence from unchanged live reruns; no Accounting Spine, reference-mode, behavioural, calibration or public-version state changes;
- aligned the retained BNR BLS manual workflows with the terminal 2025-Q2 exact-source reopen policy: the May-2025 XLSX identity probe is now explicitly historical replay only, and the missing-round recovery workflow is a frozen-candidate replay until genuinely new official evidence supplies a semantically bound 2025-Q2 target and the contract is updated; unchanged manual reruns do not count as scientific progress and cannot mutate the canonical 11/12 panel, with no model or version-state change;
- completed the AMECO legacy-workflow execution boundary after Redisstat migration hardening: the frozen pre-2022 archive probe and 2022–Spring-2026 CAPB real-time-vintage materialiser are now explicitly classified as historical fixed-vintage replay paths, their reruns cannot count as a new AMECO vintage or extend the frozen release list, and neither can serve as the future Autumn-2026 Redisstat executor; no scientific gate, data value, model state or version state changes;
- corrected the AMECO workflow execution boundary after Redisstat migration hardening: the existing manual source-probe and materialisation workflows are now explicitly classified as historical Spring-2026 legacy-ZIP re-probe/re-materialisation paths, and a rerun may not be labeled as an Autumn-2026 vintage; the migration-readiness assessment and CI require a separate preregistered Redisstat contract/workflow only after the Autumn release exists and provider dataset identity plus `UBLGBPS` semantics are verified, with no current polling, workflow creation, value inspection or scientific gate opening;
- synchronized the release-versioning rationale after AMECO access-migration hardening: the existing third Unreleased change class now explicitly includes the ECFIN Redisstat transition, frozen `UBLGBPS` semantics and unresolved provider dataset/series identity in addition to the prior BLS/MoF `NO_REOPEN` adjudications; the numeric release state remains unchanged at public/repository v0.3.0 with `version_bump_required_now = false` and v0.4.0 as the default next substantive candidate;
- advanced the canonical trigger-aware monitoring horizon to a `post_ameco_access_protocol` successor after Redisstat migration readiness was hardened: the existing AMECO trigger remains release-conditioned and closed, but its future `action_if_release_occurs` is now bound to the provider-owned browser/dataflow/catalogue discovery protocol and frozen `UBLGBPS` semantics before any query or payload can be retained; the previous `post_source_diagnostics` horizon remains immutable as immediate predecessor, all six trigger identities/dates/evidence windows are unchanged, and the Redisstat dataset/series identity remains unresolved with no value inspection, calibration or version change;
- hardened AMECO Redisstat migration readiness with exact official dataset-discovery routes rather than a guessed code: RMD now preregisters the ECFIN SDMX all-dataflows catalogue, Catalogue TOC TXT endpoint and AMECO browser root as the provider-owned ways to identify the future dataset code, requires title/structure plus frozen `UBLGBPS` semantic verification before any query is retained, and keeps the Redisstat dataset/series identity explicitly unresolved; chapter 17, `ameco17` and third-party mirror identifiers remain prohibited as inferred official dataset codes, with no current polling, value inspection, trigger or version change;
- tightened the AMECO interface-migration boundary with an explicit semantic anchor: the retained target remains `UBLGBPS` / `ROM.1.0.319.0.UBLGBPS`, and the official Spring-2026 download page links its `Cyclical Adjustment of Public Finance Variables` category to `ameco17.zip`; however, the exact Redisstat `DATASET_CODE`/series key is deliberately recorded as unresolved because the official API documentation requires that identity to be obtained from Redisstat itself, so neither chapter 17, the legacy filename nor a third-party mirror may be promoted into an official Redisstat key by inference;
- preregistered AMECO source-access migration readiness for the future Autumn 2026 fiscal trigger after the Commission announced retirement of the old AMECO Online interface before that forecast: retained historical URLs/bytes remain immutable, the Spring 2026 legacy chapter URL is explicitly not a future filename template, and any future source cycle must discover the exact release through official AMECO current-release/Archive or ECFIN Redisstat bulk/API surfaces before freezing URL/query, bytes and UBLGBPS semantics; the assessment is baseline/CI-governed and leaves the fiscal mechanism DEFERRED with no release trigger, polling, value inspection, calibration or version change;
- qualified the final remaining pre-promotion 9/10 sentence in `STATUS.md`: the ECB official-tolerance gate PASS is now explicitly labeled as an immediate post-execution historical checkpoint that authorized a separate promotion assessment, while the current successor remains 10/10; regression coverage rejects the stale present-tense `canonical state therefore remains 9/10` wording without changing any historical assessment, gate result, scientific state or version;
- completed a systematic historical/current wording pass over the pre-promotion `sectoral_financial_positions` recovery sections in `STATUS.md`: five 2026-09-21 checkpoints that correctly recorded 9/10 reference modes are now explicitly labeled as historical/pre-execution checkpoints and point forward to the separate 10/10 successor, while regression coverage rejects the unqualified stale `remains 9/10` wording; no historical assessment, scientific result, gate or version state changed;
- clarified one remaining historical/current ambiguity in `STATUS.md`: the earlier PR #48–#52 `REVIEW_READY_MERGE_DECISION_PENDING` state is now explicitly labeled as a review-ready checkpoint rather than the current repository-governance state; later integration-complete and current scientific-hold states remain unchanged, and regression coverage prevents the stale `current ... REVIEW_READY` wording from returning;
- clarified another historical/current boundary in `STATUS.md`: the completed #48–#52 integration state is now explicitly labeled as an integration checkpoint, so its then-correct statement that the 10th reference mode was not promoted cannot be read as current 9/10 state; the later separate 10/10 reference-mode promotion remains the current successor without rewriting the integration terminal assessment;
- corrected the scope metadata of the retained 2026-09-20 general reopen-trigger monitoring snapshot: the snapshot itself remains the latest full-scope pass, but it is now explicitly marked superseded for `sectoral_financial_positions` by the later 2026-09-21 BNR financial-accounts topology trigger; the scientific baseline mirrors the same scope and CI rejects either loss of the exception or accidental replacement of the historical general snapshot;
- clarified the historical/current monitoring-horizon boundary in `STATUS.md`: the 2026-09-20 horizon is now explicitly labeled as the initial historical predecessor rather than described with the stale word `now`, while the current handoff continues to point to the 2026-09-22 `post_source_diagnostics` successor; focused regression coverage preserves that distinction without changing any trigger, gate, date or version state;
- synchronized the release-versioning rationale after the latest no-reopen source diagnostics: the contract now describes three current post-v0.3.0 change classes—partial F4 scientific progress, provenance/governance hardening, and post-terminal evidence adjudication/monitoring refinement—while retaining `unreleased_changes_present = true`, `version_bump_required_now = false`, v0.3.0 as the immutable public release and v0.4.0 as the default next substantive milestone; focused tests prevent the rationale from silently dropping the new BLS/MoF no-reopen class;
- advanced the central trigger-aware monitoring horizon to a same-day `post_source_diagnostics` successor after the BNR BLS opaque-endpoint and MoF exchange-topology clues were adjudicated `NO_REOPEN`; the previous post-F4 horizon is preserved immutably, all six trigger identities and all dates/evidence windows remain unchanged, the next dated check stays 2026-10-08, routine ECB QSA 2026-10-02 remains non-triggering without topology change, and no scientific/calibration/accounting/feedback gate is opened;
- synchronized the scientific baseline's generic `trigger_aware_monitoring_horizon` authority with the already-canonical current 2026-09-22 post-F4 horizon; dated predecessor/successor authority keys remain immutable lineage, while cross-registry CI now rejects drift between the generic authority, baseline canonical state and model contract; no monitoring gate, scientific state or version changes;
- added the repository-wide provenance terminal closure to `STATUS.md` as an internal continuation handoff: the status document now points to the machine-checkable terminal assessment and current trigger-aware monitoring horizon, records that there is no active provenance repair task, and preserves the public README's user-oriented boundary; focused regression coverage prevents those handoff links from silently disappearing, with no scientific or version-state change;
- synchronized the canonical government issuance–yield next-task pointer with its already-established terminal state: `next_government_issuance_yield_empirical_task` now points to `government_issuance_yield_loop_evidence_triggered_hold` rather than the completed/interrupted missing-source recovery path, and the terminal loop audit now rejects regression to the stale pointer; no source search is reopened and no scientific or version state changes;
- kept the public README landing page user-oriented after provenance closure by removing the internal terminal-assessment/reopen-trigger paragraph from the narrative `Data and provenance` section; the machine-checkable terminal assessment remains registered in the model contract/baseline, enforced by Scientific CI and available through the local reproduction command, with no scientific or version-state change;
- closed the current repository-wide provenance-integrity hardening stage with a machine-checkable terminal assessment: 32 source-evidence directories and all 13 processed CSVs remain inventory-governed, Validation Recovery retains its explicit partial-raw-recovery boundary, the three canonical provenance authorities are parity-checked across the model contract and scientific baseline, and future provenance work reopens only on declared evidence/change/failure triggers; the terminal audit is part of Scientific CI/local reproduction and changes no scientific or version state;
- synchronized the release-versioning rationale with the actual post-v0.3.0 state: the contract now records both the partial F4 scientific progress and the subsequent provenance/reproducibility governance hardening while retaining `unreleased_changes_present = true`, `version_bump_required_now = false` and v0.4.0 as the default next substantive milestone; no version surface or scientific state changed;
- completed provenance-governance parity across the canonical model contract and scientific baseline: source-vintage inventory, processed-data inventory and validation-recovery vintage status are now all registered centrally in `model_contract.json`, and Scientific CI fails if any of those three authorities drift between the model contract and baseline; the source-vintage summary count is also corrected to 23 hash-backed local manifests plus 1 normative-semantic manifest, with no scientific or version-state change;
- synchronized the professional landing-page design contract with repository reality: the v1.0 proposal/approval boundary is retained historically, while contract v1.1 now records that the reviewed README was implemented on `main` at commit `159dcfb06b36fa510ffc8e09822b4cf7031b65a3`, the preview remains review history, and the public README is CI-governed; no visual, scientific or version change is introduced by this synchronization;
- extended README↔Scientific-CI parity tests to the processed-data inventory and validation-recovery provenance verifier, so all three user-visible provenance reproduction gates (source-vintage inventory, processed-data inventory and validation-recovery provenance) now fail tests if either README or CI drops the corresponding command;
- synchronized the release/documentation entry points after the v0.3.0 publication: corrected a stale v0.2.0 sentence in `releases/README.md`, extended the release-publication audit/tests so release-index version/date/commit drift fails CI, and added the repository-wide source-vintage inventory audit to the README local reproduction path with a README↔CI parity invariant; no version or scientific state changed;
- added a repository-wide source-vintage inventory coverage gate: all 32 top-level `data/source_vintages/` directories are now canonically classified by provenance anchor (23 hash-backed local manifests, 1 normative-semantic manifest, 6 hash-aware local audits, 1 review-only local anchor and 1 explicitly governed external review-copy anchor), the model contract and scientific baseline register the inventory, and Scientific CI now fails on any unregistered/stale vintage directory without retroactively fabricating manifests or upgrading reproducibility claims;
- advanced the central trigger-aware monitoring horizon to a 2026-09-22 successor after F4 structural-zero recovery: all existing event/source triggers are preserved, the BNR CNF 2025 S121×counterpart AF4 stock publication is now registered centrally as a stock-only evidence-window check after 2026-10-31, repeated pre-window polling is prohibited, the 2026-10-08 BNR policy decision remains the next dated event check, and no scientific gate is opened;
- synchronized the current-release sentence in `STATUS.md` with the immutable v0.3.0 publication record after detecting a stale v0.2.0 header, and extended the release-publication audit so STATUS version/date/release-commit drift now fails CI; historical v0.2.0 references remain unchanged;
- completed the MoF realized-financing provenance chain in the scientific baseline: the frozen source-vintage probe contract and retained 2025 source-vintage manifest are now explicit baseline authorities, and cross-registry CI verifies the exact `materialisation contract → probe contract → retained vintage → assessment` linkage; no scientific or version state changed;
- clarified `STATUS.md` provenance after the F4 structural-zero successor materialization: the earlier 15-stock/15-flow Accounting Spine terminal section is now explicitly marked as a historical pre-successor checkpoint and points to the current post-F4 terminal authority, avoiding ambiguity with the current 25-stock/15-flow F4 partial core; no scientific state or version changed;
- finalized the immutable v0.3.0 publication state: verified the GitHub Release/tag on exact green commit `33c11b7e10e7e837097da5b34a9251c7bedb3f2c`, recorded release ID 393215575 and publication workflow success, consumed the one-shot authorization, advanced the current public release to v0.3.0, and set v0.4.0 as the default next substantive milestone candidate without changing scientific model state;

### Scientific integrity and reproducibility

- identified a genuinely new official MoF liability-management source topology for the deferred government refinancing/effective-rate mechanism: the May 2025 exchange prospect explicitly links four old ISINs to two reopened benchmark ISINs and requires same-day publication of auction results, but the current indexed official surface still does not expose a reproducible realized-result document with accepted principal plus same-boundary opening principal and matched old/new effective rates; a post-terminal assessment therefore preserves Gate 1 `FAIL`, leaves `government_repricing_ledger.csv` unchanged, prohibits coupon/price/indicative-nominal substitution, and keeps estimation, calibration and feedback activation closed;
- adjudicated a newly indexed official-domain BNR XLSX endpoint after the 2025-Q2 BLS source-discovery stage had closed: `uploads/editor/1843119253.xlsx` is retained as a genuine new public-index clue but exposes no semantic binding to the August 2025 / realised-2025-Q2 survey round and cannot currently be retrieved reproducibly for byte/schema/date verification; a post-terminal successor therefore records `NEW_OFFICIAL_INDEXED_XLSX_ENDPOINT_SEMANTICS_UNRESOLVED_NO_REOPEN`, preserves the canonical BLS panel at 11/12 with 2025-Q2 missing, and CI now rejects promotion based only on official domain or `.xlsx` extension;
- reconciled the feedback-architecture terminal snapshot with the later 10/10 reference-mode closure without rewriting historical provenance: the original 2026-09-21 terminal assessment remains an immutable 9/10 snapshot, while a new post-reference-closure successor records 10/10 aggregate reference modes with no reference-mode blocker but preserves 0/22 exact integrated feedback links, 0/5 activation-ready structures, 0 scalar delays ready and 0 validated behavioural mechanisms; the successor is registered in the model contract/baseline and audited in Scientific CI, with no calibration, feedback, behavioural-closure or version change;
- clarified the processed-data inventory/public documentation boundary after legacy reconciliation: `data/processed` is an exhaustive governed processed-artifact surface, not a claim that all 13 files are canonical/current; the current inventory explicitly contains 3 `LEGACY_RECONCILED_BY_SUCCESSOR` artifacts alongside current and partial diagnostics, with no data, scientific-state or version change;
- reconciled the historical 13-month `monetary_pass_through_bnr_2024_2025.csv` diagnostic with the later Validation Recovery boundary: it is now explicitly retained as `LEGACY_RECONCILED_BY_SUCCESSOR`, while the current selection/holdout cycle remains bound to the separately pinned `data/raw/validation_recovery/` policy-rate, household-housing and NFC-up-to-1y inputs; no historical values, validation result, mechanism status or version state changed;
- refined source-vintage provenance classification so ordinary `LOCAL_MANIFEST` anchors must contain at least one SHA-256 identity, while the ESA 2010 normative semantic evidence is explicitly classified as `NORMATIVE_SEMANTIC_MANIFEST` because the third-party PDF is not redistributed and no numeric observations are used; CI now preserves that distinction instead of allowing an unhashed semantic citation to look like a byte-reproducible local vintage, with no change to the ESA semantic adjudication or scientific/version state;
- strengthened the repository-wide processed-data provenance gate so every declared JSON sidecar must explicitly bind the exact `data/processed` artifact it describes; all five declared sidecars now carry that binding, including the retained legacy refinancing sidecar, and CI rejects missing/mismatched bindings without changing data values, scientific classifications or version state;
- strengthened the retained validation-recovery provenance gate by pinning the exact five-file `data/raw/validation_recovery/` repository input surface (legacy fetch manifest plus four normalized statistical CSVs) to Git-blob identities and minimum schemas; the existing offline verifier now fails on added/removed files, byte drift or schema drift, while preserving the distinction between normalized-input reproducibility and the still-incomplete historical raw-provider vintage; the vintage-status registry is now an explicit scientific-baseline authority;
- added a repository-wide processed-data provenance inventory covering all 13 CSVs under `data/processed`, with pinned Git-blob identities, required provenance/schema columns and explicit links to existing sidecars, assessments and retained source-vintage manifests; the inventory preserves each artifact's declared current/partial/legacy role, and Scientific CI fails on unregistered processed CSVs, silent byte changes, missing authority anchors or missing declared source-vintage manifests without upgrading scientific status or authorizing live refetch;
- screened the ECB International Reserves (RAS) central-bank F4 loan series as a possible F4 stock constraint and rejected it as a canonical reopen path: the Romanian series is monthly reserve-template data, foreign-currency-only, nominal-value and aggregated to counterpart S1Q (`other sectors than MFIs`), whereas canonical F4 is quarterly QSA/ESA, all-currency, standard-valuation and requires the exact BNR-holder split across H/C/F/G/X; no rank/materialization change occurs and the BNR CNF 2025 counterpart-stock publication remains the priority F4 trigger;
- separated the next F4 stock-specific reopen trigger from the exhausted quarterly/reference-mode paths: the existing BNR CNF annual counterpart matrix is semantically valid for exact year-end stock counterpart evidence but the retained/currently reviewed matrix covers 2024, not 2025; a future official 2025 S121×counterpart AF4 stock matrix can reopen only the F4 stock materialization gate, while flow/reference-mode boundaries remain unchanged; monitoring is frozen as `MONITOR_PUBLICATION_NO_REOPEN` with an evidence-window check only after end-October 2026, not a presumed publication date;
- adjudicated the retained zero non-consolidated BNR F4 liability stock as an exact stock-only structural constraint under ESA 2010 creditor/debtor and from-whom-to-whom balance-sheet semantics, without extending aggregate zero transactions to bilateral flow zeros;
- preregistered and executed an offline successor F4 partial materialization from the retained Phase A/B vintage: exact stock coverage increases from 15 to **25 of 35** in-boundary cells (rank 31/nullity 4), flow remains **15 of 35** (rank 26/nullity 9), the ten remaining stock cells preserve the unresolved 6.1 million RON BNR asset-counterpart allocation, the historical `f4_partial_2025.json` artifact remains immutable, and `benchmark_2025.json` is unchanged;
- returned Accounting Spine recovery to evidence-triggered hold with F3 still the only canonically complete stock-and-flow instrument; no calibration, feedback activation, behavioural closure or version bump is authorized;

## 0.3.0 - 2026-09-21

### Scientific integrity and reproducibility

- consolidated the Accounting Spine recovery cycle into a terminal evidence-triggered hold assessment: F3 remains the only canonically complete stock+flow instrument; F2, F4 and F5-F8 retain explicit partial/frozen dispositions and reopen triggers; no selective reopen or unconditional accounting-recovery task remains active; README now exposes this state and the new terminal audit is part of Scientific CI/local reproduction;
- synchronized `scientific_baseline_manifest.json` with the canonical release contract after detecting stale 0.1.0/0.2.0 release fields despite the published v0.2.0 release; manifest v1.2 now records current public/repository version 0.2.0, next candidate 0.3.0, registers the new F2/F4/F5/F6/F7/F8 Accounting Spine boundary authorities, and Scientific CI now verifies baseline release-versioning fields against `release_versioning_contract.json` so the same drift cannot remain green; the canonical release rationale was also refreshed to describe substantive post-v0.2.0 reference-mode/accounting-boundary progress while retaining `version_bump_required_now = false`;
- closed the repeated standard-BPM6/Eurostat/BNR F2 source-search path after a standards-level diagnostic showed that Direct-Investment debt instruments are not required to be disseminated by specific instrument in standard IIP/BOP presentations: currency/deposits may conceptually be inside DI debt, but Eurostat and BNR public presentations retain the broad DI debt aggregate, so neither broad debt instruments nor the residual gap can identify the missing Direct-Investment F2 share; F2M remains materialized, F21/total-F2 remain incomplete, and reopening now requires a country-specific supplementary DI-F2 source or an equivalent exact bridge;
- executed the preregistered F8 OECD counterpart Stage 2 gate from retained official source bytes: 180 stock and 720 flow rows were complete and 48+48 resident A/L mirror checks passed exactly, but the frozen 0.1 million RON S1 aggregate-consistency gate failed in 33/66 stock checks (max 1.0m RON) and 46/66 annual-flow checks (max 4.0m RON), while F8=F81+F89 also failed in 24/60 stock, 27/60 annual-flow and 47/240 quarterly-flow checks; execution therefore stopped before rank recomputation, no new F8 equations were admitted, the conditional BNR zero-stock scenario remains unpromoted, and F8 remains frozen at rank 11/nullity 24 with zero unconditional unique bilateral cells;
- reopened F8 selectively at the source-topology boundary because the retained OECD quarterly from-whom-to-whom artifact publishes total F8 plus F81/F89 by reporting/counterpart sector for Romania; Stage 2 extraction, A/L mirror, S1 aggregation, F8=F81+F89 and exact-rank criteria are preregistered before detailed value review, while S12 remains F+BNR because S121 is absent and the historical conditional BNR-zero stock scenario remains explicitly unpromoted;
- executed the preregistered F7 OECD counterpart Stage 2 gate from retained official source bytes: 180 stock and 720 flow rows were complete and 48+48 resident A/L mirror checks passed exactly, but the frozen 0.1 million RON S1 aggregate-consistency gate failed in 4/66 stock checks (max 1.0m RON) and 19/66 annual-flow checks (max 2.0m RON), while F7=F71+F72 also failed in 2/60 stock, 3/60 annual-flow and 9/240 quarterly-flow checks; execution therefore stopped before rank recomputation, no new F7 equations were admitted, and F7 remains frozen at stock rank 11/nullity 24 and flow rank 10/nullity 25 with zero unique bilateral cells;
- reopened F7 selectively at the source-topology boundary because the retained OECD quarterly from-whom-to-whom artifact publishes total F7 plus F71/F72 by reporting/counterpart sector for Romania; Stage 2 extraction, A/L mirror, S1 aggregation, F7=F71+F72 and exact-rank criteria are preregistered before detailed value review, while S12 remains F+BNR because S121 is absent and no F7 materialisation is authorized by the reopen itself;
- executed the preregistered F6 OECD total/component counterpart Stage 2 gate from retained official source bytes: 420 stock and 1,680 flow rows were complete and 112+112 resident A/L mirror checks passed exactly, but the frozen 0.1 million RON S1 aggregate-consistency gate failed in 7/154 stock checks (max 1.0m RON) and 13/154 annual-flow checks (max 2.0m RON), while F6=F61+F62+F63+F64+F65+F66 also failed in 4/60 stock, 11/60 annual-flow and 32/240 quarterly-flow checks; execution therefore stopped before rank recomputation, no new F6 equations were admitted, and F6 remains frozen at rank 11/nullity 24 with zero unique bilateral cells;
- reopened F6 selectively at the source-topology boundary because the retained OECD quarterly from-whom-to-whom artifact publishes total F6 and all F61-F66 components by reporting/counterpart sector for Romania; a Stage 2 extraction, A/L mirror, S1 aggregation, F6=sum(F61..F66) and exact-rank gate is preregistered before value review, while S12 remains F+BNR because S121 is absent and no F6 materialisation is authorized by the reopen itself;
- executed the preregistered F5 OECD F512/F519 counterpart Stage 2 gate on the retained source artifact: 600 required 2025 rows were complete and A/L resident mirrors reconciled exactly, but the frozen 0.1 million RON S1 aggregate-consistency gate failed in 16/44 stock checks (max 1.0 million RON) and 24/44 flow checks (max 3.0 million RON); execution therefore stopped before rank recomputation, no OECD equations were admitted, and F5 remains frozen at rank 73/nullity 67 with zero unconditional F51/F5 total cells;
- reopened F5 selectively at the source-topology boundary after the already-retained OECD quarterly from-whom-to-whom artifact was found to publish exact F512 (unlisted shares) and F519 (other equity) counterpart rows for Romania; Stage 2 value/reconciliation and exact-nullspace criteria are preregistered before detailed residual/rank review, S12 remains only an F+BNR aggregate because S121 is absent, and no F5 cell, benchmark, calibration state or behavioural closure is changed by the reopen itself;
- tested the retained Eurostat `nasq_10_f_cp` F4 counterpart boundary against retained ECB BSI MFIs-excluding-NCB loan series as a possible exact BNR bridge; 2025-Q4 NFC/household stock residuals are about 307.05/244.74 million EUR and 2025 transaction residuals about -1400.09/-1560.17 million EUR, while official ECB methodology documents conceptual differences between financial-accounts and BSI loan measures; the subtraction is therefore rejected as an exact accounting identity, no new F4 rank equation is admitted, and the 6.1 million RON BNR asset-counterpart allocation remains unresolved;
- promoted the final aggregate `sectoral_financial_positions` reference mode to `OBSERVED_SERIES_AVAILABLE` after an independent ECB national-QFA internal-consistency rule supplied the previously required domain-specific quantitative tolerance: exact-decimal instrument-level residuals are at most 0.2 million RON across F2-F8 stocks and flows, far below the official EUR 10 million national-data threshold; reference-mode observability is now 10/10 while the bilateral Accounting Spine, calibration, feedback activation and behavioural closure remain unchanged;
- executed the preregistered ECB QFA per-instrument horizontal-consistency gate: 686 exact-decimal F2-F8 stock/flow quarter tests passed the deliberately stricter <10 million RON sufficient bound, official ECB quarterly RON/EUR average/end-period series remained above 4.38 across the full span, and the maximum per-instrument residual was 0.2 million RON; the gate authorizes only a separate reference-mode promotion assessment, so readiness remains 9/10 until that assessment is completed;
- added an offline window-stability diagnostic for the corrected OECD reconciliation: zero of ten possible contiguous 40-quarter windows pass the frozen 0.1 million RON identity gate (39–41 violations per window), and failures persist in 2025Q1–2026Q1; post-result window shifting or period dropping is therefore explicitly prohibited;
- corrected the retained semantic-adjusted OECD reconciliation count using exact decimal arithmetic without rerunning any source: 16 binary-float boundary false positives at exactly ±0.1 million RON reduced the mathematically valid violation count from 63 to 47 (21 flows, 26 stocks); the frozen tolerance and FAIL verdict are unchanged, and the historical workflow artifact remains immutable;
- completed a post-terminal OECD dissemination-precision diagnostic without rerunning values: generic SDMX `DECIMALS=2` can envelope the six-sector system residual under a conservative one-ULP-per-observation bound, but fails the independent `S1+S2` diagnostic (0.28 million RON envelope versus ~0.5 million observed); lexical one-decimal CSV formatting is not treated as an official precision rule, so no tolerance change or 10/10 promotion is authorized;
- completed the independent ESA 2010 semantic-adjusted OECD gate for `sectoral_financial_positions`: the sole `S1M/L/F2` gap was treated as a preregistered normative structural zero, the exact row and finite-value gates passed, but the frozen 0.1 million RON system-reconciliation tolerance failed in 63 of 98 measure-quarter tests (max ~0.5 million flows, ~0.6 million stocks); no post-result tolerance relaxation is authorized, so readiness remains 9/10 and the path returns to evidence-triggered hold;
- identified an official NBR/CMFB 2025 financial-accounts structural change and executed one preregistered public-access recovery pass: OECD non-consolidated quarterly stock/flow topology passed with S121 and F2-F8 coverage across 49 common quarters, but the frozen exact-row gate failed because `S1M/L/F2` is absent in all 49 quarters for both stock and flow; the gate stopped before `OBS_VALUE` review or reconciliation, so `sectoral_financial_positions` remains partial at 9/10 readiness and Accounting Spine/behavioural state is unchanged;

### Repository governance

- superseded the 2026-09-20 trigger-aware monitoring horizon with a dated 2026-09-21 successor after the OECD/ESA precision chain closed; no gate is currently open, the next dated check remains the 2026-10-08 BNR policy decision, and future sectoral-financial-position precision recovery now explicitly requires new domain-specific official precision/balancing evidence rather than residual-driven tolerance changes;
- finalized the post-publication v0.2.0 governance state: recorded the immutable GitHub release/tag identity, consumed the one-shot publication authorization, advanced the current public release to v0.2.0 and set v0.3.0 as the default next substantive milestone candidate without changing any scientific model state;

## 0.2.0 - 2026-09-21

### Scientific integrity and reproducibility

- formalized and independently audited numerical-integration convergence for Dynamic Core primitives: constant-rate stock updates are partition-invariant, a first-order Euler delay converges under repeated time-step halving, and the System Dynamics adequacy rule remains `dt/tau < 1/3`; with the default quarterly `dt=0.25` this means a future active first-order delay must have `tau > 0.75 years` or use a smaller time step, while integrated-model numerical robustness remains explicitly unclaimed because behavioural closure is inactive;
- added a cross-structure feedback-architecture terminal gate after link-level review of all five registered structures: 22 registered links, 0 exact integrated link equations ready, 0 activation-ready structures and all structures on evidence-triggered hold; structural-review completion explicitly does not authorize parameter estimation, feedback activation, behavioural closure, model-completion claims or a version change;
- completed the external FX–refinancing structural review as an explicit **open chain**: all four links remain non-integrated, the required joint currency-denomination × residual-maturity × hedging exposure is unresolved, debt service is kept distinct from refinancing need, risk pricing remains multi-driver, no exchange-rate-pressure closing link is inferred, and the structure is placed on evidence-triggered hold without behavioural closure or version change;
- completed the structural review cycle for `monetary_credit_transmission_loop`: all five links now have explicit boundaries but 0/5 exact integrated equations are ready; the household pass-through remains target-specific and unvalidated, rate-to-credit and credit-to-expenditure mappings remain unidentified, HICP is not promoted to a core reference mode, the policy rate stays observed/exogenous, and the loop enters evidence-triggered hold with no active empirical task;
- reviewed `inflationary_pressure → policy_rate`: the BNR inflation target and actual inflation are observed, but formation-date expectations, real-time activity-gap vintages and simultaneity/regime identification remain insufficient for a policy rule; the policy rate stays observed/exogenous and no Taylor-rule closure is introduced;
- reviewed `private_demand_and_investment → inflationary_pressure`: observed HICP remains diagnostic evidence rather than a new Dynamic Core reference mode; inflation is multi-driver and the demand contribution is not identified, so no demand-to-inflation equation or core inflation extension is introduced;
- reviewed `credit_flow → private_demand_and_investment`: aggregate NFC/household credit transactions finance multiple purposes, including working capital, refinancing and house purchase, so they cannot be relabelled as household consumption or NFC fixed investment without an observed purpose-allocation bridge;
- reviewed `market_and_lending_rates → credit_flow`: MIR rates are price variables, MIR new-business volumes are agreement volumes, and the retained ECB BSI credit flow is a distinct realized net-transaction concept jointly shaped by supply and demand; no rate-to-flow identity or implicit elasticity is introduced;
- reviewed `policy_rate → market_and_lending_rates`: the frozen household-housing pass-through remains target-specific and unvalidated, with no NFC form or generic market/outstanding-rate mapping; no target-specific form is promoted into the generic monetary-transmission link;
- completed the structural review cycle for `bank_credit_balance_sheet_loop`: all four links now have explicit boundaries but 0/4 exact integrated equations are ready; supply-demand identification, debt-service/risk alignment, prudential population matching and both delay parameters remain blocked, so the loop enters evidence-triggered hold with no active empirical task;
- reviewed `credit_supply_capacity → credit_flow`: realized ECB BSI credit transactions reflect both lending supply conditions and borrower demand, so credit flow is not a supply-only measure and no capacity-to-flow identity, inferred-demand shortcut or synthetic sector aggregation is admitted;
- reviewed `debt_service_and_credit_risk → credit_supply_capacity`: NPL/DSTI, prudential capital/funding states and BLS lending standards are related but non-identical dimensions; no scalar credit-supply-capacity measure, exogenous NPL shock or synthetic prudential index is promoted;
- reviewed the `loan_stock → debt_service_and_credit_risk` boundary: outstanding credit is exposure scale, not debt-service burden or NPL state; aggregate NPL and narrower housing-loan DSTI evidence remain non-interchangeable, so the combined risk node stays unresolved and no proxy-based feedback equation is introduced;
- reviewed the bank-credit `credit_flow → loan_stock` boundary on the exact ECB BSI private non-financial credit universe: published financial transactions are not stock first differences, non-transaction adjustments remain required, the loan-stock reference target is frozen to `credit_stock`, and no endogenous stock equation or feedback activation is introduced;
- closed the current government issuance-yield candidate loop at the present evidence boundary: 0/5 links have exact integrated equations, all five nodes remain unresolved, the supply-source path remains trigger-gated with three official acts missing, the tested sovereign-yield form remains FAIL_BEFORE_HOLDOUT, both delays remain TBD, and the loop enters evidence-triggered hold with no active empirical task;
- closed the current government refinancing-interest candidate loop as structurally reviewed but quantitatively blocked: all four links now have explicit boundary decisions, 0/4 have exact integrated equations, all four nodes remain unresolved, the maturity delay and repricing share remain unidentified, and the loop enters evidence-triggered hold with no active empirical task;
- reviewed the government-debt-stock to interest-cost link as a conditional accounting scale relation: Eurostat apparent cost confirms the matched S13 accrued-interest/average-Maastricht-debt boundary, while year-end debt, Ministry portfolio rate and current market yields remain non-substitutable; no new reference mode, parameter, equation activation or behavioural closure is introduced;
- Review the government gross-borrowing/issuance to Maastricht-debt-stock bridge: reject one-to-one accumulation, anchor the 2025 S13 stock change to Eurostat EDP Table 3 stock-flow reconciliation, keep matched net debt transactions unidentified, and preserve inactive feedback/behavioural closure.
- retained all twelve 2025 Ministry monthly public-debt reports and extracted exact provider-published cumulative-YTD financing channels; changing FX-conversion regimes, exchange-operation semantics and non-monotone published lines block month-to-month differencing, so no monthly realized-financing flow series or channel allocation is promoted;
- froze a Ministry realized-financing channel source contract for provider-published cumulative-YTD actual borrowing by instrument/market; raw source retention and exact cumulative extraction are authorized, while monthly differencing, residual channel inference, synthetic allocation shares and cross-vintage FX conversion assumptions remain blocked;
- reviewed the bridge from government financing need to debt issuance as an explicit multi-channel allocation problem: the retained BNR domestic primary-market series remains one channel only, the Ministry 45%/55% split is scoped to deficit financing rather than total GFN, no residual or synthetic channel shares are allowed, and the generic financing-need/debt-issuance link remains unresolved;
- reviewed the government-interest-cost to financing-need boundary as a conditional accounting composition rather than a behavioural equation: headline-deficit GFN forms already include interest, primary-deficit forms include interest exactly once, and no cross-boundary Eurostat/MoF identity or canonical financing-need reference mode is promoted;
- reviewed the sovereign-yield to government-interest-cost boundary and rejected any contemporaneous one-to-one mapping from the ECB 10-year RON yield to portfolio cost or interest expenditure; Ministry portfolio-average rate and Eurostat D41PAY remain distinct observed targets, while the repricing/refinancing transition stays unidentified and feedback remains inactive;
- closed repeated full-2025 announced-supply source retries as an evidence-triggered hold: eight legally identified Ministry orders remain raw-source unavailable because Portal Legislativ transport is not reproducible from GitHub Actions and no exact alternative MF/ANAF raw copy was discovered; no values are imputed, canonical promotion remains blocked, and the next independent issuance-yield task is a structural primary-yield-to-sovereign-yield boundary review;
- retained exact official Ministry/ANAF source PDFs for April, May-base, June and October 2025 and materialised a competitive-only announced-RON reference-auction candidate with six exact final months (January-April, June and October); May remains non-final because OMF 752 is not retained, July-September and November-December remain unavailable, and canonical reference-mode promotion stays blocked;
- reviewed the full-2025 definition of announced RON primary-market supply and found a structural break in SSON information timing: the exact Q1 combined competitive+fixed-SSON pilot remains retained as legacy evidence, while the full-year comparable candidate is narrowed to competitive reference-auction targets only and must preserve May/November/December amendment history before any canonical promotion;
- retained the January-March 2025 Ministry issuance prospectuses and materialised an exact event-level pilot for announced RON domestic primary-market supply: completed-period monthly sums are RON 5,770m, 8,040m and 8,165m; a RON 75m SSON scheduled for 1 April remains a forward event rather than being shifted into March. Canonical reference-mode promotion remains deferred pending the April-December 2025 extension;
- froze the Ministry event-level source contract for the first government-securities supply reference mode: domestic RON discount Treasury-bill auctions, benchmark reference auctions and benchmark SSON targets remain separate source components; monthly aggregation uses each event date, Article 1 totals must reconcile exactly, and no stock denominator, accepted amount, yield-effect estimate or feedback activation is introduced;
- redesigned the unresolved government-securities supply-pressure concept as a structured measurement vector that separates announced RON supply level, announcement surprise, duration supply, auction absorption/bid-to-cover and secondary-market liquidity/capacity context; no scalar composite is selected, and the first recovery priority is the exact announced-RON primary-supply reference mode without a stock denominator;
- probed and retained preregistered ECB denominator candidates for the RON auction supply-load design: the CSEC S1311 domestic-currency debt-securities stock is valid but market-valued, the exact preregistered GFS domestic-currency face-value key returns no series, and the valid GFS face-value series is S13/all-currency context only; no denominator or supply-load ratio is selected;
- reviewed the denominator boundary for the proposed RON primary-market supply-load diagnostic: Ministry nominal RON holdings, ECB CSEC central-government market-value stocks and ECB GFS general-government face-value stocks remain distinct candidates, with no one-to-one auction-universe denominator selected and no pressure ratio materialised;
- retained three exact Ministry of Finance source PDFs for the government-securities supply-pressure probe and verified native PDF-text extractability without OCR: the January auction table supplies exact completed-month announced/borrowed values, while submitted-bid/bid-to-cover chart series and the stock denominator remain explicitly blocked pending exact mapping/boundary reconciliation;
- reviewed the observable boundary for government-securities supply pressure using official Ministry primary/secondary-market reporting: retained a multidimensional candidate design separating ex-ante announced supply load, auction absorption, published bid-to-cover and secondary-market liquidity, while explicitly prohibiting raw issuance, accepted amounts or inverse bid-to-cover from being promoted as a scalar pressure node;
- materialised the retained BNR 2025 domestic-primary-market issue rates by instrument and currency, preserving nil/not-applicable source symbols and distinct yield/interest-rate semantics; no cross-instrument aggregate, generic sovereign-yield mapping, causal issuance-to-yield claim or feedback activation is introduced;
- preregistered the government issuance–yield empirical boundary: retained the qualitative closed candidate topology while explicitly rejecting one-to-one substitution of BNR domestic-primary-market issuance into the generic closed loop; four separate bridges remain required for financing-channel allocation, supply-pressure operationalisation, primary-yield mapping and debt-stock interest-cost propagation;
- retained the exact official BNR December 2025 Monthly Bulletin PDF used by the full-year domestic-primary-market government-securities issuance pilot, with fixed-URL acquisition, repository-retained raw bytes, byte count and SHA-256 provenance; this closes only the raw-source-retention blocker and leaves the generic issuance node unresolved;
- completed a later-vintage BNR revision check for the domestic-primary-market issuance pilot: all 28 Jan-Jul native-currency amount cells are unchanged at published precision, and the reviewed pilot now covers all twelve months of 2025; the retained raw source closes provenance retention while structural narrowing of the issuance-yield path remains a separate gate;
- retained the original reviewed Jan-Jul 2025 BNR domestic-primary-market government-securities pilot lineage with native RON/EUR columns and complete Q1-Q2 aggregation, subsequently extended and revision-checked to full-year 2025 without collapsing the BNR boundary into total government borrowing;
- screened official BNR and Ministry of Finance issuance/borrowing sources and identified `government_debt_issuance` as semantically overloaded across the two government feedback loops; froze separate domestic-primary-market and all-market borrowing materialisation paths while keeping the generic node unresolved;
- added a government F3 holder-composition and net-incurrence diagnostic for 2025, explicitly distinguishing financial-account net incurrence from gross issuance/refinancing need and keeping holder absorption separate from the unresolved securities-supply-pressure feedback node;
- added deterministic F3 observed behavior-over-time diagnostics for 2025, decomposing sector and bilateral Q1-Q4 stock changes into Q2-Q4 financial transactions plus combined non-transaction changes while explicitly preserving the 9/10 partial sectoral-financial-positions reference-mode boundary;
- added a deterministic F3-only quarterly empirical replay for 2025-Q2..Q4 using retained QSA stocks and transactions plus an explicitly non-causal combined non-transaction residual; the replay closes 105 bilateral transitions exactly at published precision without changing full-RMD accounting readiness;
- added a machine-checked 14-criterion activation matrix for every registered feedback structure; only explicit `PASS` counts, leaving all five structures quantitatively blocked under the current evidence boundary;
- implemented the exact frozen household-housing delta-policy monetary candidate as a target-specific executable form while preserving CANDIDATE status and keeping the generic monetary-credit feedback link quantitatively inactive;
- froze behavioural parameter sign semantics for current executable forms: sensitivity betas are non-negative magnitudes and equation operators own causal signs, with runtime rejection of silent negative-coefficient sign reversals;
- added an evidence gate for all seven candidate delays; timing distributions, survey cadence, lag windows, average maturity and average re-fixing are explicitly prevented from being reused as scalar `tau` values without exact validation;
- added exact link-level feedback readiness and a per-loop activation matrix; all 22 registered path links and all five feedback structures remain quantitatively blocked rather than treating related mechanisms or qualitative topology as integrated equations;
- added form-level behavioural extreme-condition and structural-sensitivity tests while explicitly keeping integrated extreme-condition validation and empirical sensitivity execution blocked;
- added an executable feedback-variable boundary gate covering every qualitative loop/chain node and preventing topology or observed-target availability from silently implying endogeneity;
- added an executable behavioural dimensional-consistency gate that separates symbolic unit validity from unfrozen empirical measurement scales and blocks activation without inventing transforms;
- added executable behavioural-implementation governance so retained exploratory equations cannot be mistaken for currently admitted mechanism implementations;
- completed scientific-baseline consolidation and integration with executable cross-registry consistency checks;
- retained exact source vintages and strengthened offline provenance verification so accepted historical evidence does not depend on future provider availability;
- completed the fiscal structural-primary real-time source adjudication and preserved the reviewed AMECO raw source vintage without later refetch;
- returned the fiscal mechanism to `DEFERRED` after the source-only reopen, without estimation, target-family substitution or final-evaluation inspection;
- added post-closure reopen-trigger monitoring and a trigger-aware monitoring horizon that prevents repeated unchanged-source probing from being treated as scientific progress;
- preserved the Evidence-Triggered Baseline Hold, inactive behavioural closure and zero validated reference behavioural mechanisms.

### Accounting and reference-mode recovery

- strengthened Accounting Spine readiness and retained instrument-specific rank/materialization boundaries;
- completed current-public-source recovery screening for the remaining sectoral-financial-position reference-mode blocker;
- retained explicit reopen conditions instead of using synthetic allocations or relaxed source boundaries.

### Repository governance

- added a review-gated RMD social-preview design brief using GitHub's 1280x640 recommendation, suite grayscale identity and explicit scientific non-claim constraints; no social-preview image is published by this change;
- added PR-only workflow concurrency so superseded Scientific CI runs on the same pull-request branch are canceled while main/manual runs retain unique execution groups;
- added RMD community-health governance with contribution/support guidance, structured bug and scientific issue forms, and a pull-request scientific-integrity checklist; security policy and code-of-conduct adoption remain explicitly deferred pending private-reporting/enforcement decisions;
- defined a professional scientific-model README design contract and a canonical-state-checked RMD landing-page preview, without replacing the public README before review;
- added a light/dark, accessibility-checked RMD conceptual-structure diagram preview that shows the institutional boundary and accounting/evidence/dynamics/activation layers without implying validated causal links or complete bilateral coverage;
- prepared the professional RMD first-page implementation with compact suite-consistent header, canonical scientific-status panel, responsive concept diagram, reproduction path, provenance rules, repository map and reader routing; publication remains review-gated;
- added an atomic release/versioning contract and release checklist;
- future version changes must synchronize package metadata, README badge, citation metadata, changelog, release contract, Git tag and GitHub Release.

## 0.1.0 - 2026-09-18

Initial public scientific-core baseline.

### Included

- accounting spine and structural dynamic-core contracts;
- empirical-dynamics and mechanism/evidence registries;
- source and processed data with provenance;
- validation-recovery selection, holdout and disposition artifacts;
- government repricing ledger contract and assessment;
- Python scientific/reference implementation;
- reproducibility and empirical-recovery scripts;
- standardized RMD project identity and release metadata.

### Scientific status

The model is in Validation Recovery / Empirical Basis Expansion. The canonical disposition records zero validated reference behavioural mechanisms. Monetary pass-through forms remain candidates or fail selection gates, and the government repricing mechanism remains deferred because the required boundary-matched structure is not point-identifiable from the current public data.

### Scope boundary

The release is a scientific model core, not a calibrated macroeconomic forecast, production policy simulator, or end-user application.
